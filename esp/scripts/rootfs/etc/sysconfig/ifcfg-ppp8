IFNAME=ppp8
AUTOSTART=no
ifPrep() {
  modprobe ppp_async && modprobe ppp_deflate &&
  pppd call t-mobile unit 8 logfile /dev/null
}

hosts() {    #add lines before EOS
  cat <<EOS 
$(netIfIP $IFNAME $(hostname)-$IFNAME $(hostname)-ppp)
EOS
}
