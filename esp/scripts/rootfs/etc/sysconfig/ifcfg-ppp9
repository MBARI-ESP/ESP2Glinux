IFNAME=ppp9
AUTOSTART=no
ifPrep() {
  modprobe arc4 && modprobe ppp_mppe_mppc &&
  pppd unit 9 call mbari name scwharf logfile /dev/null
}

hosts() {    #add lines before EOS
  cat <<EOS 
$(netIfIP $IFNAME $(hostname)-$IFNAME $(hostname)-vpn)
EOS
}
