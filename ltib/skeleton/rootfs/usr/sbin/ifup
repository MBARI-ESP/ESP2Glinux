#!/bin/sh
#
#  ifup [interface name|definition file]
#
. /usr/share/ifupfn.sh

trap gateUpdated EXIT TERM INT HUP QUIT

tracefn=/tmp/ifup.trace
if [ -w $tracefn ]; then
  (date; echo "$@"; env) >>$tracefn
  exec 2>>$tracefn
  set -x
fi

ifUp "$@"
