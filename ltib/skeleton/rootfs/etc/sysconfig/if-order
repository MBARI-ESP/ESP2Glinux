# Define interface start and stop order on boot & shutdown
#  Interfaces in START list are started before NFS and login allowed
#  Interfaces in STOP list are stopped in sequence before others
#  Interfaces in START are stopped last in reverse order

START='lo wired'
STOP='ppp6'
