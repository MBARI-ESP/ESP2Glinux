IFNAME=ppp5
AUTOSTART=no

#VPN=$ESPshore/ppp6   #server IP / VPN interface

ifPrep() {
  pppd call iridium unit ${IFNAME#ppp} updetach
}

hosts() {    #add lines before EOS
  cat <<EOS
$(netIfIP $IFNAME $(hostname)-$IFNAME $(hostname)-iridium)
EOS
}
