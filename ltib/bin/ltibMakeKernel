#/!bin/sh
[ -z "$DEV_IMAGE" ] && . `dirname $0`/ltibenv
cd $KERNELDIR && {
  case "$1" in
    *config)
      exec /usr/bin/make ARCH=arm "$@"
      ;;
    *)
      exec /usr/bin/make -j4 INSTALL_MOD_PATH=${INSTALL_MOD_PATH:-$DEV_IMAGE} \
        ARCH=arm  HOSTCC="ccache /usr/bin/gcc -B/usr/bin/" KDIR=$KERNELDIR "$@"
  esac
}
